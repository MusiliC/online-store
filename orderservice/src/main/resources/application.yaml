server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ordersdb
    username: root
    password: makutano
  application:
    name: order-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update