server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MusiliC/spring-microservices-config
          default-label: main
          skipSslValidation: true